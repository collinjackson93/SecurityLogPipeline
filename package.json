{
  "name": "securitylogpipeline",
  "version": "1.0.0",
  "description": "A pipeline for sending log info to ElasticSearch. Intended to be used for security monitoring purposes.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "meld": "^1.3.2"
  }
}
